{
    "shell_cmd": "gnome-terminal -e 'bash -c \"g++ ${file} -o ${file_path}/${file_base_name}; ${file_path}/${file_base_name}; echo;echo;echo Press ENTER to exit; read line;exit; exec bash;\"'",
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
    "working_dir": "${file_path}",
    "selector": "source.c, source.c++",
}